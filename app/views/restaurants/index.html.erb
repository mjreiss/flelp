<div class ="jumbotron">
  <h2>Flelp is the best way to find reviews on local restaurants.</h2>
  <h3>Explore the favorite diners, pastry shops, and eateries in your community.
  <br>Leave reviews and ratings for your fellow foodies.</h3>
</div>

<table class="table table-hover table-condensed">
  <thead>
    <tr>
      <th>Name</th>
      <th>Address</th>
      <th>Phone</th>
      <th>Website</th>
      <th colspan="3"></th>
    </tr>
  </thead>

<tbody>
  <% @restaurants.each do |restaurant| %>
    <tr>
      <td><%= link_to restaurant.name, restaurant %></td>
      <td><%= restaurant.address %></td>
      <td><%= restaurant.phone %></td>
      <td><%= link_to restaurant.website, "http://"+restaurant.website %></td> 
      <% if current_user && current_user.admin? %>
      <td><%= link_to 'Edit', edit_restaurant_path(restaurant), class: "btn btn-xs btn-link" %></td>
      <td><%= link_to 'Destroy', restaurant, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-xs btn-link" %></td>
      <% end %>
    </tr>
  <% end %>
</tbody>
</table>

<br>
<% if current_user && current_user.admin? %>
  <%= link_to 'New Restaurant', new_restaurant_path %>
<% end %>
<%= image_tag 'https://s3.amazonaws.com/flelpprod/happy_footer.png' %>
